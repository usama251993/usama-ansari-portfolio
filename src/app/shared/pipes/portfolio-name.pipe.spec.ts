import { PortfolioNamePipe } from './portfolio-name.pipe';

describe('PortfolioNamePipe', () => {
  it('create an instance', () => {
    const pipe = new PortfolioNamePipe();
    expect(pipe).toBeTruthy();
  });
});
