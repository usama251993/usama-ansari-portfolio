import { PortfolioDateMonthPipe } from './portfolio-date.pipe';

describe('DateDurationPipe', () => {
  it('create an instance', () => {
    const pipe = new PortfolioDateMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
